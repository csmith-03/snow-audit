# Copy this file to .env.local and fill in your own values.
# .env.local is gitignored — never commit real credentials.

# Optional. Without it, the app still runs — /upload degrades to static-only
# findings (no AI summary or semantic findings). See src/lib/analyzers/aiSummary.ts.
ANTHROPIC_API_KEY=
